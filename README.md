# HexoBackUo
我的Hexo 備份

執行 hexoc.sh 加上指令 s 或 d

```bash
./hexoc.sh s
```

會一次性執行

```bash
hexo clean && hexo g && hexo s 
```

```bash
./hexoc.sh d
```

則會一次性執行

```bash
hexo clean && hexo g && hexo d 
```
